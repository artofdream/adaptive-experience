apiVersion: 1

datasources:
  - name: CloudWatch
    type: cloudwatch
    access: proxy
    uid: cloudwatch
    isDefault: true
    jsonData:
      authType: default
      defaultRegion: us-east-1

  - name: Prometheus
    type: prometheus
    access: proxy
    uid: prometheus
    url: http://agent-runner.aea-pilot.internal:8080
    isDefault: false
    jsonData:
      httpMethod: GET
      timeInterval: 15s
