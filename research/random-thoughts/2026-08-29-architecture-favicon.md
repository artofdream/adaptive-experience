# Architecture Pages favicon

#aea

Related #284 (favicon slice only). Skip link / :focus-visible / tap targets stay on #284.

Gold portal SVG on charcoal. Distinct from the florist lily on aea.artof.link (#285).
Conflicts with !313 on `scripts/build_framework_site.py`. MRC rebases. Do not batch. Do not merge from implementer.
