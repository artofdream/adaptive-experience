# Antifragility is end to end (29 Aug 2026)

Related: #299. Sponsor: the rule is not only for the framework or Path B. It applies to the repo operating loop as well.

This is a **scope** statement. AEA is still **not** claiming to be antifragile.

## Three surfaces, one rule

When the same miss repeats, change a sensor or a gate, not another paragraph.

1. Framework site (architecture.artof.link).
2. Instantiation / Path B (aea.artof.link).
3. How the work is run: issues, MRs, parallel runners, merge hat.

Public comparison principle 4 now names that scope.

## Counter-examples (not trophies)

- Path B: dual-viewport after CSS remains Unknown.
- Operating loop: #282 asked for a confirm. !325 implemented and closed it. Pipeline 1238 success is CI, not a live ARM64 probe.

## What this does not do

- Does not implement #297 (path and scope claim). That stays evaluate-only until the team comments.
- Does not add a fifth public principle.
- Does not invent IDs. Does not restyle the shop. Does not edit DATE_RE.
