.DS_Store
*.tmp
*.bak
__pycache__/
*.py[cod]
.venv/

# Obsidian (local vault workspace — do not publish)
.obsidian/
.trash/
.space/

# Local secrets (never commit real endpoint/key/model values)
.env
.env.*
!.env.example
*.tfvars
!*.tfvars.example

# Terraform plugin cache / working directory (never commit)
.terraform/
**/.terraform/
tfplan
*.tfplan
*.tfstate
*.tfstate.*
crash.log
crash.*.log
.terraform.tfstate.lock.info
